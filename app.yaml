version: "2"

services:
  
  ant-design-vue:
    container_name: ant-design-vue
    image: xiaodi93/ant-design:latest
    networks:
      - default
    restart: always

networks:
  default:
    external:
      name: server
